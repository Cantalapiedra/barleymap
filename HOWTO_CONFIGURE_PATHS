HOWTO_CONFIGURE_PATHS is part of Barleymap.
Copyright (C)  2013-2014  Carlos P Cantalapiedra.
(terms of use can be found within the distributed LICENSE file).

# Created 20180111 (updated 20180111)

This HOWTO describes the file conf/paths.conf and how to configure it.
This file is essential to run barleymap.

It should include the next fields:

# important fields to configure

app_path -> the path to the barleymap installation directory. E.g. /apache/var/html/barleymap/
tmp_files_path -> the path to the folder where barleymap will store temporary files (must have permissions). E.g. /apache/var/html/barleymap/tmp_files/
datasets_path -> path to the folder where the pre-computed datasets are stored
annot_path -> path to the folder where annotation of datasets is stored
maps_path -> path to the folder where the maps data are stored

# fields to configure depending on the sequence aligner to be used
# (you could choose to use only one of them, or several, or all of them)

blastn_app_path -> path to the blastn command.
blastn_dbs_path -> path to the folder where the blastn databases are stored.
gmap_app_path -> path to the gmap command.
gmap_dbs_path -> path to the folder where the gmap databases are stored.
gmapl_app_path -> path to the gmapl command.
hsblastn_app_path -> path to the hsblastn command.
hsblastn_dbs_path -> path to the folder where the hsblastn databases are stored.

# fields which are usually untouched

genmap_path -> the path to the auxiliary script to create the graphical chromosomes. Usually just "app_aux/".
split_blast_path -> the path to the auxiliary script to run blast processes in parallel. Usually just "app_aux/".
citation -> how the users of the aplication should cite it. Usually Cantalapiedra_CP,_Boudiar_R,_Casas_AM,_Igartua_E,_Contreras-Moreira_B._BARLEYMAP:_physical_and_genetic_mapping_of_nucleotide_sequences_and_annotation_of_surrounding_loci_in_barley._Mol_Breeding_(2015)_35:13_DOI_10.1007/s11032-015-0253-1
stdalone_app -> URL to the standalone version of barleymap. Usually just "https://github.com/Cantalapiedra/barleymap"

# Note that you could use "#" to include comments in the file.

## An example of a conf/paths.conf file could be:

app_path /apache/var/html/barleymap/
tmp_files_path /apache/var/html/barleymap/tmp_files
# aux data and files
genmap_path app_aux/
split_blast_path app_aux/
citation Cantalapiedra_CP,_Boudiar_R,_Casas_AM,_Igartua_E,_Contreras-Moreira_B._BARLEYMAP:_physical_and_genetic_mapping_of_nucleotide_sequences_and_annotation_of_surrounding_loci_in_barley._Mol_Breeding_(2015)_35:13_DOI_10.1007/s11032-015-0253-1
stdalone_app https://github.com/Cantalapiedra/barleymap
# data paths
datasets_path /home/bmapuser/datasets/
annot_path /home/bmapuser/datasets_annotation/
maps_path /home/bmapuser/maps/
# aligners
blastn_app_path /home/bmapuser/ncbi-blast/bin/blastn
blastn_dbs_path /home/bmapuser/blast_dbs/
gmap_app_path /home/bmapuser/gmap_aligner/bin/gmap
gmap_dbs_path /home/bmapuser/gmap_dbs/
gmapl_app_path /home/bmapuser/gmap_aligner/bin/gmapl
hsblastn_app_path /home/bmapuser/hs-blastn-0.0.5+/hs-blastn-src/hs-blastn
hsblastn_dbs_path /home/bmapuser/hsblastn_dbs/

#